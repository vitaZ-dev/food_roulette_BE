
#DB
spring:
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 12MB

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://${DB_HOST}:3306/team_c?allowMultiQueries=true
    username: ${DB_USERNAME}
    password: ${DB_USERPASSWORD}

  #mybatis


mybatis:
  type-aliases-package: com.green.food_roulette
  mapper-locations: mapper/**/*.xml

#SWAGGER
springdoc:

  swagger-ui:
    path: /swagger.html
    operations-sorter : method


file:
  dir: d:\download\
